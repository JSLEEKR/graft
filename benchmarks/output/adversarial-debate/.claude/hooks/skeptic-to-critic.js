#!/usr/bin/env node
// Auto-generated by Graft Compiler
// Edge: Skeptic -> Critic

const fs = require('fs');
const path = require('path');

const INPUT = path.resolve('.graft/session/node_outputs/skeptic.json');
const OUTPUT = path.resolve('.graft/session/node_outputs/skeptic_to_critic.json');
const TOKEN_LOG = path.resolve('.graft/token_log.txt');

if (!fs.existsSync(INPUT)) {
  // Graceful no-op: this hook fires on ALL Write calls,
  // so the source output may not exist yet (different agent writing).
  process.exit(0);
}

const raw = fs.readFileSync(INPUT, 'utf-8');
const data = JSON.parse(raw);

let result = {
  "approach": data["approach"],
  "edge_cases": data["edge_cases"],
  "failure_modes": data["failure_modes"],
  "convergence_score": data["convergence_score"]
};

const output = JSON.stringify(result, null, 2);
fs.writeFileSync(OUTPUT, output);

// Token accounting
const original = Buffer.byteLength(raw);
const transformed = Buffer.byteLength(output);
const reduction = original > 0 ? Math.round((original - transformed) * 100 / original) : 0;
const timestamp = new Date().toISOString().replace('T', ' ').slice(0, 19);

fs.appendFileSync(TOKEN_LOG, `[${timestamp}] Edge Skeptic->Critic | ${original}B -> ${transformed}B (${reduction}% reduction)\n`);
