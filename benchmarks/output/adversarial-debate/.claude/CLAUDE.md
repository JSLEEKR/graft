# Graft Orchestration: AdversarialDebate

> Auto-generated by Graft Compiler. Edit the .gft source, not this file.

## Budget
Total: 120,000 tokens
Best case: 133,498 tokens
Worst case: 191,498 tokens

## Persistent Memory
- `DebateHistory`: `.graft/memory/debatehistory.json` (4,000 tokens max)
- Memories persist across runs. Nodes with `writes` clauses update memory after execution.


## Execution Plan

### Step 1: [parallel] Architect, Pragmatist, Skeptic, Specialist
- **Dispatch all 4 agents concurrently** using the Agent tool in a single message
- Wait for all to complete before proceeding
- Agent: architect -- tokens: input ~6,000 / output ~5,000 [mem-read: debatehistory]
- Agent: pragmatist -- tokens: input ~6,000 / output ~4,000 [mem-read: debatehistory]
- Agent: skeptic -- tokens: input ~6,000 / output ~4,000 [mem-read: debatehistory]
- Agent: specialist -- tokens: input ~6,000 / output ~4,000 [mem-read: debatehistory]
- Completion: all 4 `===NODE_COMPLETE===` signals received

### Step 2: Critic [sequential]
- Agent: critic
- Inputs: `.graft/session/node_outputs/architect_to_critic.json`, `.graft/session/node_outputs/pragmatist_to_critic.json`, `.graft/session/node_outputs/skeptic_to_critic.json`, `.graft/session/node_outputs/specialist_to_critic.json`
- **Edge transform** (Architect → Critic): keep only fields: `approach`, `tradeoffs`, `convergence_score`. Run: `node .claude/hooks/architect-to-critic.js` or manually apply. Output: `.graft/session/node_outputs/architect_to_critic.json`
- **Edge transform** (Pragmatist → Critic): keep only fields: `approach`, `tradeoffs`, `convergence_score`. Run: `node .claude/hooks/pragmatist-to-critic.js` or manually apply. Output: `.graft/session/node_outputs/pragmatist_to_critic.json`
- **Edge transform** (Skeptic → Critic): keep only fields: `approach`, `edge_cases`, `failure_modes`, `convergence_score`. Run: `node .claude/hooks/skeptic-to-critic.js` or manually apply. Output: `.graft/session/node_outputs/skeptic_to_critic.json`
- **Edge transform** (Specialist → Critic): keep only fields: `approach`, `domain_insights`, `convergence_score`. Run: `node .claude/hooks/specialist-to-critic.js` or manually apply. Output: `.graft/session/node_outputs/specialist_to_critic.json`
- Expected tokens: input ~15,698 / output ~6,000
- Completion: `===NODE_COMPLETE:critic===`
- Output: `.graft/session/node_outputs/critic.json`

### Step 3: Converger [sequential]
- Agent: converger
- Input: `.graft/session/node_outputs/critic_to_converger.json`
- **Edge transform**: After Critic completes, transform its output: minify JSON (no whitespace). Save to `.graft/session/node_outputs/critic_to_converger.json` before starting Converger.
- Also reads: `.graft/session/node_outputs/architect.json` (ArchitectProposal)
- Also reads: `.graft/session/node_outputs/pragmatist.json` (PragmatistProposal)
- Also reads: `.graft/session/node_outputs/skeptic.json` (SkepticProposal)
- Also reads: `.graft/session/node_outputs/specialist.json` (SpecialistProposal)
- Expected tokens: input ~21,200 / output ~5,000
- Completion: `===NODE_COMPLETE:converger===`
- Output: `.graft/session/node_outputs/converger.json`

### Step 4: Implementer [sequential]
- Agent: implementer
- Input: `.graft/session/node_outputs/converger_to_implementer.json`
- **Edge transform**: After Converger completes, transform its output: keep only fields: `adopted_approach`, `final_code`, `test_plan`. Save to `.graft/session/node_outputs/converger_to_implementer.json` before starting Implementer.
- Expected tokens: input ~6,500 / output ~8,000
- Completion: `===NODE_COMPLETE:implementer===`
- Output: `.graft/session/node_outputs/implementer.json`

### Step 5: Reviewer [sequential]
- Agent: reviewer
- Input: `.graft/session/node_outputs/implementer_to_reviewer.json`
- **Edge transform**: After Implementer completes, transform its output: minify JSON (no whitespace). Save to `.graft/session/node_outputs/implementer_to_reviewer.json` before starting Reviewer.
- Also reads: `.graft/session/node_outputs/converger.json` (ConvergedDesign)
- Expected tokens: input ~10,600 / output ~5,000
- Completion: `===NODE_COMPLETE:reviewer===`
- Output: `.graft/session/node_outputs/reviewer.json`

### Step 6: Conditional routing from Reviewer
- **Automatic**: Router hook evaluates conditions on Reviewer's output
- Routing file: `.graft/session/routing/reviewer_route.json`
- Read the `target` field and proceed accordingly:
  - If `approved` == `false`: run **Implementer** agent (tokens: input ~6,500 / output ~8,000)
  - If else (default): run **Converger** agent (tokens: input ~21,200 / output ~5,000)
- Each branch agent reads from `.graft/session/node_outputs/reviewer.json`

## Token Budget Tracking
Check `.graft/token_log.txt` after each step.
- 80% consumed: switch remaining agents to compact mode
- 90% consumed: skip non-critical agents

## Failure Recovery
- Agent failure: follow on_failure policy in each agent definition
- Token overrun: switch to compact mode, then skip non-critical steps
- Complete failure: intermediate results preserved in `.graft/session/`
