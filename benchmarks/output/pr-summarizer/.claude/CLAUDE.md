# Graft Orchestration: PRSummarizer

> Auto-generated by Graft Compiler. Edit the .gft source, not this file.

## Budget
Total: 15,000 tokens
Best case: 13,889 tokens
Worst case: 13,889 tokens

## Execution Plan

### Step 1: Analyzer [sequential]
- Agent: analyzer
- Expected tokens: input ~4,000 / output ~3,000
- Completion: `===NODE_COMPLETE:analyzer===`
- Output: `.graft/session/node_outputs/analyzer.json`

### Step 2: Formatter [sequential]
- Agent: formatter
- Input: `.graft/session/node_outputs/analyzer_to_formatter.json`
- **Edge transform**: After Analyzer completes, transform its output: keep only fields: `summary`, `risk_level`, `breaking_changes`, then minify JSON (no whitespace). Save to `.graft/session/node_outputs/analyzer_to_formatter.json` before starting Formatter.
- Expected tokens: input ~5,889 / output ~1,000
- Completion: `===NODE_COMPLETE:formatter===`
- Output: `.graft/session/node_outputs/formatter.json`

## Token Budget Tracking
Check `.graft/token_log.txt` after each step.
- 80% consumed: switch remaining agents to compact mode
- 90% consumed: skip non-critical agents

## Failure Recovery
- Agent failure: follow on_failure policy in each agent definition
- Token overrun: switch to compact mode, then skip non-critical steps
- Complete failure: intermediate results preserved in `.graft/session/`
