# Graft Orchestration: DataAnalysis

> Auto-generated by Graft Compiler. Edit the .gft source, not this file.

## Budget
Total: 50,000 tokens
Best case: 32,360 tokens
Worst case: 32,360 tokens

## Execution Plan

### Step 1: Classifier [sequential]
- Agent: classifier
- Expected tokens: input ~4,000 / output ~1,000
- Completion: `===NODE_COMPLETE:classifier===`
- Output: `.graft/session/node_outputs/classifier.json`

### Step 2: [parallel] StatAnalyzer, TrendAnalyzer
- **Dispatch all 2 agents concurrently** using the Agent tool in a single message
- Wait for all to complete before proceeding
- Agent: statanalyzer -- tokens: input ~4,420 / output ~3,000 [input: `.graft/session/node_outputs/classifier_to_statanalyzer.json`]
- Agent: trendanalyzer -- tokens: input ~4,420 / output ~3,000 [input: `.graft/session/node_outputs/classifier_to_trendanalyzer.json`]
- **Edge transform** (Classifier → StatAnalyzer): keep only fields: `data_type`, `columns`, then minify JSON (no whitespace). Run: `node .claude/hooks/classifier-to-statanalyzer.js` or manually apply. Output: `.graft/session/node_outputs/classifier_to_statanalyzer.json`
- **Edge transform** (Classifier → TrendAnalyzer): keep only fields: `data_type`, `columns`, then minify JSON (no whitespace). Run: `node .claude/hooks/classifier-to-trendanalyzer.js` or manually apply. Output: `.graft/session/node_outputs/classifier_to_trendanalyzer.json`
- Completion: all 2 `===NODE_COMPLETE===` signals received

### Step 3: ReportWriter [sequential]
- Agent: reportwriter
- Inputs: `.graft/session/node_outputs/statanalyzer_to_reportwriter.json`, `.graft/session/node_outputs/trendanalyzer_to_reportwriter.json`
- **Edge transform** (StatAnalyzer → ReportWriter): keep only fields: `summary_stats`, `outliers`, then minify JSON (no whitespace). Run: `node .claude/hooks/statanalyzer-to-reportwriter.js` or manually apply. Output: `.graft/session/node_outputs/statanalyzer_to_reportwriter.json`
- **Edge transform** (TrendAnalyzer → ReportWriter): keep only fields: `trends`, `correlations`, then minify JSON (no whitespace). Run: `node .claude/hooks/trendanalyzer-to-reportwriter.js` or manually apply. Output: `.graft/session/node_outputs/trendanalyzer_to_reportwriter.json`
- Also reads: `.graft/session/node_outputs/classifier.json` (Classification)
- Expected tokens: input ~7,520 / output ~5,000
- Completion: `===NODE_COMPLETE:reportwriter===`
- Output: `.graft/session/node_outputs/reportwriter.json`

## Token Budget Tracking
Check `.graft/token_log.txt` after each step.
- 80% consumed: switch remaining agents to compact mode
- 90% consumed: skip non-critical agents

## Failure Recovery
- Agent failure: follow on_failure policy in each agent definition
- Token overrun: switch to compact mode, then skip non-critical steps
- Complete failure: intermediate results preserved in `.graft/session/`
