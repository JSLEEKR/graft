# Graft Orchestration: AdversarialReview

> Auto-generated by Graft Compiler. Edit the .gft source, not this file.

## Budget
Total: 40,000 tokens
Best case: 28,360 tokens
Worst case: 28,360 tokens

## Execution Plan

### Step 1: [parallel] SecurityReviewer, LogicReviewer, PerformanceReviewer
- **Dispatch all 3 agents concurrently** using the Agent tool in a single message
- Wait for all to complete before proceeding
- Agent: securityreviewer -- tokens: input ~3,000 / output ~3,000
- Agent: logicreviewer -- tokens: input ~3,000 / output ~3,000
- Agent: performancereviewer -- tokens: input ~3,000 / output ~2,000
- Completion: all 3 `===NODE_COMPLETE===` signals received

### Step 2: SeniorReviewer [sequential]
- Agent: seniorreviewer
- Inputs: `.graft/session/node_outputs/securityreviewer_to_seniorreviewer.json`, `.graft/session/node_outputs/logicreviewer_to_seniorreviewer.json`, `.graft/session/node_outputs/performancereviewer_to_seniorreviewer.json`
- **Edge transform** (SecurityReviewer → SeniorReviewer): keep only fields: `vulnerabilities`, `severity`, then minify JSON (no whitespace). Run: `node .claude/hooks/securityreviewer-to-seniorreviewer.js` or manually apply. Output: `.graft/session/node_outputs/securityreviewer_to_seniorreviewer.json`
- **Edge transform** (LogicReviewer → SeniorReviewer): keep only fields: `bugs`, `edge_cases`, then minify JSON (no whitespace). Run: `node .claude/hooks/logicreviewer-to-seniorreviewer.js` or manually apply. Output: `.graft/session/node_outputs/logicreviewer_to_seniorreviewer.json`
- **Edge transform** (PerformanceReviewer → SeniorReviewer): keep only fields: `hotspots`, `complexity_concerns`, then minify JSON (no whitespace). Run: `node .claude/hooks/performancereviewer-to-seniorreviewer.js` or manually apply. Output: `.graft/session/node_outputs/performancereviewer_to_seniorreviewer.json`
- Expected tokens: input ~6,360 / output ~5,000
- Completion: `===NODE_COMPLETE:seniorreviewer===`
- Output: `.graft/session/node_outputs/seniorreviewer.json`

## Token Budget Tracking
Check `.graft/token_log.txt` after each step.
- 80% consumed: switch remaining agents to compact mode
- 90% consumed: skip non-critical agents

## Failure Recovery
- Agent failure: follow on_failure policy in each agent definition
- Token overrun: switch to compact mode, then skip non-critical steps
- Complete failure: intermediate results preserved in `.graft/session/`
