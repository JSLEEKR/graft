# Graft Orchestration: ContentPipeline

> Auto-generated by Graft Compiler. Edit the .gft source, not this file.

## Budget
Total: 40,000 tokens
Best case: 23,110 tokens
Worst case: 23,110 tokens

## Persistent Memory
- `PublishedArticles`: `.graft/memory/publishedarticles.json` (3,000 tokens max)
- Memories persist across runs. Nodes with `writes` clauses update memory after execution.


## Execution Plan

### Step 1: Researcher [sequential]
- Agent: researcher
- Memory load: `.graft/memory/publishedarticles.json`
- Expected tokens: input ~3,800 / output ~3,000
- Completion: `===NODE_COMPLETE:researcher===`
- Output: `.graft/session/node_outputs/researcher.json`

### Step 2: Drafter [sequential]
- Agent: drafter
- Input: `.graft/session/node_outputs/researcher_to_drafter.json`
- **Edge transform**: After Researcher completes, transform its output: keep only fields: `key_points`, `angle`, then minify JSON (no whitespace). Save to `.graft/session/node_outputs/researcher_to_drafter.json` before starting Drafter.
- Expected tokens: input ~2,060 / output ~4,000
- Completion: `===NODE_COMPLETE:drafter===`
- Output: `.graft/session/node_outputs/drafter.json`

### Step 3: Editor [sequential]
- Agent: editor
- Input: `.graft/session/node_outputs/drafter_to_editor.json`
- **Edge transform**: After Drafter completes, transform its output: keep only fields: `title`, `body`. Save to `.graft/session/node_outputs/drafter_to_editor.json` before starting Editor.
- Expected tokens: input ~3,200 / output ~5,000
- Completion: `===NODE_COMPLETE:editor===`
- Output: `.graft/session/node_outputs/editor.json`

### Step 4: MetadataExtractor [sequential]
- Agent: metadataextractor
- Input: `.graft/session/node_outputs/editor_to_metadataextractor.json`
- **Edge transform**: After Editor completes, transform its output: keep only fields: `title`, then minify JSON (no whitespace). Save to `.graft/session/node_outputs/editor_to_metadataextractor.json` before starting MetadataExtractor.
- Memory save: `.graft/memory/publishedarticles.json`
- Expected tokens: input ~1,050 / output ~1,000
- Completion: `===NODE_COMPLETE:metadataextractor===`
- Output: `.graft/session/node_outputs/metadataextractor.json`

## Token Budget Tracking
Check `.graft/token_log.txt` after each step.
- 80% consumed: switch remaining agents to compact mode
- 90% consumed: skip non-critical agents

## Failure Recovery
- Agent failure: follow on_failure policy in each agent definition
- Token overrun: switch to compact mode, then skip non-critical steps
- Complete failure: intermediate results preserved in `.graft/session/`
