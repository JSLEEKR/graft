# Graft v4.5 Implementation Plan

## Overview

Comparison operators + conditional expressions. 3 rounds, ~8 agent calls estimated.

## Rounds

### R1: Comparison Operators (MEDIUM, A2+A3)

**Files to modify**:
- src/parser/ast.ts -- Extend binary op union with comparison operators
- src/parser/parser.ts -- Add parseComparison precedence level
- src/runtime/expr-eval.ts -- Handle comparison ops in binary case
- src/analyzer/types.ts -- Comparison result is boolean, ordered comparisons warn on non-numeric

**Test file**: tests/v45-r1.test.ts

**Test targets** (~15 tests):
- Parse: a > b, a < b, a >= b, a <= b, a == b, a != b
- Precedence: a + b > c parses as (a + b) > c
- Evaluate: numeric comparisons
- Evaluate: string equality
- Evaluate: loose equality (number == string)
- Type: comparison result is boolean
- Type: ordered comparison on string operands -> warning
- Integration: let binding with comparison compiles

### R2: Conditional Expressions (MEDIUM, A2+A3)

**Files to modify**:
- src/lexer/tokens.ts -- Add Then keyword
- src/lexer/lexer.ts -- Add 'then' to KEYWORDS
- src/parser/ast.ts -- Add conditional Expr kind
- src/parser/parser.ts -- Parse if-then-else in expression context
- src/runtime/expr-eval.ts -- Evaluate conditional expression
- src/analyzer/types.ts -- Infer conditional result type
- src/analyzer/graph-checker.ts -- Recurse into conditional parts

**Test file**: tests/v45-r2.test.ts

**Test targets** (~15 tests):
- Parse: if A.score > 50 then "high" else "low"
- Parse: nested conditional
- Parse: conditional with function call
- Evaluate: truthy condition returns consequent
- Evaluate: falsy condition returns alternate
- Evaluate: numeric condition (0 is falsy)
- Type: both branches same type -> that type
- Type: branches different types -> unknown
- Scope: recurse into condition, consequent, alternate
- Integration: let binding with conditional compiles

### R3: Integration + Regression Tests (TEST-ONLY)

**Test file**: tests/v45-r3.test.ts

**Test targets** (~10 tests):
- Cross-feature: comparison in template interpolation
- Cross-feature: conditional with template result
- Cross-feature: conditional with function call in branches
- Regression: existing operators still work
- Regression: existing builtins still work
- Scale: complex pipeline with comparisons + conditionals compiles

## Summary

| Round | Tier | Agent Calls | New Tests | Files Modified |
|-------|------|-------------|-----------|----------------|
| R1 | MEDIUM (A2+A3) | ~4 | ~15 | 4 |
| R2 | MEDIUM (A2+A3) | ~4 | ~15 | 7 |
| R3 | TEST-ONLY | ~1 | ~10 | 0 (tests only) |
| **Total** | | **~9** | **~40** | |

**Target test count**: ~1,163 (1,123 + 40)
