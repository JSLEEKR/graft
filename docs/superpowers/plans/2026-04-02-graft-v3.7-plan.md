# Graft v3.7 Implementation Plan

**Date**: 2026-04-02
**Source**: v3.6→v3.7 retro_technical.md + retro_process.md
**Process**: R-PROC-13 (MEDIUM=ambiguity), R-PROC-14 (wait for analysis), R-PROC-16 (position-absolute assertions)

## Retro-Driven Items

- TD-02: findDeclNamePosition uses hardcoded KEYWORD_LENGTHS instead of SourceLocation.length
- TD-04: server.ts workspace orchestration duplicated across 3 handlers (394 lines, approaching 400 threshold)
- Runtime gaps deferred since v3.2/v3.3: foreach failure, multi-hop conditional routing

## Rounds

### R1: Server.ts Cleanup + Reference Position Fix (DIRECT)

**Justification for DIRECT**: Mechanical refactoring (extraction) + straightforward fix (use existing field). No design ambiguity.

**Scope**:
- Extract shared workspace scanning logic from server.ts into a helper function (used by rename, references, code-actions handlers)
- Fix findDeclNamePosition in references.ts to use `loc.length` instead of KEYWORD_LENGTHS map
- Apply R-PROC-16: all new/modified range tests must assert absolute positions

**Test target**: ~6 new tests

### R2: Foreach Source Failure Handling (MEDIUM)

**Justification for MEDIUM**: Design ambiguity — how should foreach interact with the source node's failure strategy? What if the source has retry? Should foreach wait for retries? What about skip — does the foreach body run with empty data?

**Apply R-PROC-14**: Wait for A2+A3 analysis completion before convergence.

**Scope**:
- Define foreach behavior when source node fails
- Update flow-runner.ts foreach execution path
- Handle interaction with failure strategies (retry, fallback, skip, abort)

**Test target**: ~10 new tests

### R3: Multi-hop Conditional Edge Routing (MEDIUM)

**Justification for MEDIUM**: Design ambiguity — how to handle chains, cycles, and depth limits. How should the estimator account for multi-hop paths?

**Apply R-PROC-14**: Wait for A2+A3 analysis completion before convergence.

**Scope**:
- Update flow-runner.ts conditional edge evaluation to follow chains
- Add max hop depth (10) with error on exceeded
- Update estimator.ts to trace multi-hop conditional paths for cost calculation

**Test target**: ~10 new tests

### R4: Integration + Regression Tests (TEST-ONLY)

**Test target**: ~12-15 new tests (per process retro recommendation)

## Budget

| Round | Tier | Est. Agent Calls | Est. Tests |
|-------|------|-----------------|------------|
| R1 | DIRECT | 2 | 6 |
| R2 | MEDIUM | 4 | 10 |
| R3 | MEDIUM | 4 | 10 |
| R4 | TEST-ONLY | 2 | 12-15 |
| **Total** | | **~12** | **~38-41** |

**Test target**: 790 + ~38-41 = ~828-831 tests
