# Graft v4.8 Implementation Plan — LSP Expression Intelligence

## Round Summary

| Round | Feature | Complexity | Agent Count | New Tests (est.) |
|-------|---------|-----------|-------------|-----------------|
| R1 | Variable hover + go-to-definition | MEDIUM | 2 (A2+A3) | ~15 |
| R2 | Expression context completions | DIRECT | 1 | ~10 |
| R3 | Integration + regression | TEST-ONLY | 1 | ~8 |

**Estimated total**: ~33 new tests, ~4 agent calls

## Round Details

### R1: Variable Hover + Go-to-Definition (MEDIUM)

**Files to modify**:
- `src/lsp/features/hover.ts`: Add variable hover with inferred type
- `src/lsp/features/definition.ts`: Add variable go-to-definition
- `src/lsp/server.ts`: Potentially adjust caching for variable resolution

**Test file**: `tests/v48-r1.test.ts`
- Hover on variable name shows type
- Hover on variable reference shows type
- Go-to-def on variable jumps to let declaration
- Variables from different graphs don't leak

### R2: Expression Context Completions (DIRECT)

**Files to modify**:
- `src/lsp/features/completions.ts`: Add expression-context completions

**Test file**: `tests/v48-r2.test.ts`
- After `let x = `: node names, variable names, builtins, keywords
- After `if `: expression completions
- After `then `: expression completions

### R3: Integration + Regression (TEST-ONLY)

**Test file**: `tests/v48-r3.test.ts`
