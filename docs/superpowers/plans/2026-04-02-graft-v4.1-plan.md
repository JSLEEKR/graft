# Graft v4.1 Implementation Plan — Quality & Expression Hardening

## Overview

Quality release. Fix MEDIUM tech debt, harden runtime, extract growing modules. No new syntax.

**Estimated rounds**: 4
**Estimated agent calls**: ~16

## Rounds

### R1: conditionFieldName fix + evaluateCondition hardening (MEDIUM)

**Complexity**: MEDIUM (touches runtime + test migration)
**Agent count**: 2 (A2-Pragmatist + A3-Skeptic)

**Tasks**:
- Replace `output[conditionFieldName(condition)]` path in `evaluateCondition` with `evaluateExpr(condition.left, outputs, variables)` for ALL LHS types
- Remove the special-case single-segment variable check (lines 78-88 of flow-runner.ts) — `evaluateExpr` already handles variable-first resolution
- Add multi-segment condition tests: `when(Analyzer.output.score >= 80): NextNode`
- Deprecate or remove `conditionFieldName()` if no longer needed
- Verify all existing condition tests still pass

**Test target**: ~8-10 new tests
- Multi-segment condition LHS (2+ segments)
- Variable LHS in condition (still works after refactor)
- Literal field name condition (backward compat)
- Edge case: undefined intermediate segment
- Cross-feature: multi-segment condition + edge transforms

**Retro items addressed**: TD-NEW-01 (conditionFieldName multi-segment bug)

---

### R2: Graph call output isolation + division warning (DIRECT)

**Complexity**: DIRECT (contained runtime changes)
**Agent count**: 1

**Tasks**:
- Change graph_call case in flow-runner.ts to clone outputs map for child context
- Define merge behavior: child results flow through NodeResults, parent doesn't see child's intermediate outputs
- Add division-by-zero warning path (emit to errors array or separate warnings)
- Add exhaustive `never` default to all 3 FlowNode switch statements

**Test target**: ~6-8 new tests
- Child node doesn't overwrite parent same-named output
- Child can still read parent outputs (passed via clone)
- Division by zero emits warning
- Exhaustive switch: compile-time only (no runtime test needed)

**Retro items addressed**: TD-NEW-05 (shared outputs), TD-NEW-03 (silent div-by-zero), P-03 (exhaustive switch)

---

### R3: Scope checker extraction (DIRECT)

**Complexity**: DIRECT (pure refactor, no behavior change)
**Agent count**: 1

**Tasks**:
- Extract from scope.ts to `src/analyzer/graph-checker.ts`:
  - `checkGraphRecursion`, `collectGraphCalls`, `dfsGraphCycles`
  - `checkGraphCallArgs`, `checkVarCollision`, `checkExprSources`
  - `checkLiteralParamType` (module-level helper)
- ScopeChecker delegates to GraphChecker
- All existing tests must pass unchanged (pure refactor)

**Test target**: 0 new tests (refactor-only, existing tests validate)

**Retro items addressed**: scope.ts growth (694 lines → ~400 + ~300)

---

### R4: Integration + Regression (TEST-ONLY)

**Complexity**: TEST-ONLY
**Agent count**: 1

**Test target**: ~8-10 new tests
- Cross-feature: multi-segment condition + graph call + variable
- Regression: all v4.0 patterns unchanged after output isolation
- Regression: conditionFieldName removal doesn't break edge transform chains
- Scale: complex pipeline with all v4.0 + v4.1 features

## Summary

| Round | Tier | Est. Tests | Est. Calls |
|-------|------|-----------|-----------|
| R1 | MEDIUM | 8-10 | ~6 |
| R2 | DIRECT | 6-8 | ~4 |
| R3 | DIRECT | 0 | ~3 |
| R4 | TEST-ONLY | 8-10 | ~3 |
| **Total** | | **22-28** | **~16** |
