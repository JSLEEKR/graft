# Graft v3.6 Implementation Plan

**Date**: 2026-04-02
**Source**: v3.5→v3.6 retro_technical.md + retro_process.md
**Process**: R-PROC-13 (MEDIUM=ambiguity), R-PROC-14 (wait for analysis — first real test), R-PROC-15 (A3 backlog empty)

## Monitoring Items (from v3.5 retro)

- Two consecutive versions without adversarial analysis — R1 is MEDIUM to address this
- R-PROC-14 untested — R1 will be the first real test
- TEST-ONLY budget raised to 12-15 (from 10) per process retro recommendation

## Rounds

### R1: Find All References (MEDIUM)

**Justification for MEDIUM**: Find-all-references has design ambiguity — should it reuse collectRenameLocations directly or build a separate reference finder? How should cross-file references be represented? Should it find field references (reads/writes partial access)? These are genuine design questions per R-PROC-13. Also generates fresh A3 adversarial analysis (monitoring item from v3.5 retro).

**Apply R-PROC-14**: Wait for both A2 and A3 analysis to complete before starting convergence+implementation. This is the first real test of R-PROC-14.

**Scope**:
- Add `referencesProvider: true` to server capabilities
- Implement `textDocument/references` handler in server.ts (thin wrapper)
- Create `findReferences()` in features/references.ts (pure function)
- Reuse collectRenameLocations + comment/string/import filtering
- Cross-file: scan workspace export cache for importing files
- Handle `context.includeDeclaration` flag
- Return Location[] (LSP protocol)

**Test target**: ~14 new tests

### R2: Keyword Unification + Conflict Detection Fix (DIRECT)

**Justification for DIRECT**: TD-01 (keyword derivation) and TD-02 (parse-based conflict check) are spec-constrained fixes with no design ambiguity. R-PROC-12 applied.

**Scope**:
- Derive GRAFT_KEYWORDS from lexer KEYWORDS (Object.keys minus type keywords)
- Replace regex-based cross-file conflict detection in buildRenameEdits with ProgramIndex-based check
- Update tests to verify keyword synchronization

**Test target**: ~8 new tests

### R3: Symbol Range + Rename Polish (DIRECT)

**Justification for DIRECT**: Symbol range fix follows LSP spec exactly. Rename field collision guard is a straightforward check. R-PROC-12 applied.

**Scope**:
- Fix makeSymbol to compute full declaration range (start of declaration to start of next, or EOF)
- Add rename guard: warn (not block) when new name matches a field name in a referenced context
- Update document symbol tests for range accuracy

**Test target**: ~8 new tests

### R4: Integration + Regression Tests (TEST-ONLY)

**Test target**: ~12-15 new tests (raised per process retro recommendation)

## Budget

| Round | Tier | Est. Agent Calls | Est. Tests |
|-------|------|-----------------|------------|
| R1 | MEDIUM | 4 | 14 |
| R2 | DIRECT | 2 | 8 |
| R3 | DIRECT | 2 | 8 |
| R4 | TEST-ONLY | 2 | 12-15 |
| **Total** | | **~10** | **~42-45** |

**Test target**: 739 + ~42-45 = ~781-784 tests
