# Graft v4.3 Implementation Plan

## Overview

Arithmetic completion + runtime polish. 3 rounds, ~8 agent calls estimated.

## Rounds

### R1: Multiplication/Modulo + New Builtins (MEDIUM, A2+A3)

**Files to modify**:
- `src/lexer/tokens.ts` — Add Star, Percent tokens
- `src/lexer/lexer.ts` — Lex `*` and `%`
- `src/parser/ast.ts` — Add `*` and `%` to binary op union, add abs/round/keys to BUILTIN_FUNCTIONS
- `src/parser/parser.ts` — New `parseMultiplicative` precedence level between additive and unary
- `src/runtime/flow-runner.ts` — evaluateExpr: handle `*`, `%` in binary, abs/round/keys in call
- `src/analyzer/types.ts` — inferExprType: `*`/`%` → number, abs/round → number, keys → unknown
- `src/lsp/features/hover.ts` — FUNC_DOCS for abs/round/keys

**Test file**: `tests/v43-r1.test.ts`

**Test targets** (~18 tests):
- Parse: `a * b`, `a % b`, precedence (`a + b * c` = `a + (b * c)`)
- Evaluate: multiplication, modulo, modulo by zero
- Parse + evaluate: abs(-5), round(3.7), keys({a:1, b:2})
- Scope: unknown function → SCOPE_UNKNOWN_FUNCTION (unchanged)
- Type: abs/round arity validation
- Integration: pipeline with `*`, `%`, abs, round

### R2: Expression Extraction + str() Fix + Exhaustive Switches (DIRECT)

**New file**: `src/runtime/expr-eval.ts` — evaluateExpr, resolveNestedField, builtin dispatch

**Files to modify**:
- `src/runtime/flow-runner.ts` — Import from expr-eval.ts, remove extracted code
- `src/runtime/transforms.ts` — Import resolveNestedField from expr-eval.ts
- `src/runtime/transforms.ts` — Add never default to applyOne switch

**Test file**: `tests/v43-r2.test.ts`

**Test targets** (~8 tests):
- str() on object → JSON string
- str() on array → JSON string
- str() on undefined → "undefined"
- Imports from expr-eval.ts work correctly
- Existing evaluateExpr behavior preserved

### R3: Integration + Regression Tests (TEST-ONLY)

**Test file**: `tests/v43-r3.test.ts`

**Test targets** (~10 tests):
- Cross-feature: multiplication in let binding
- Cross-feature: keys() + len() composition
- Regression: existing +/-/ operators, existing builtins
- Scale: complex pipeline with all operators and functions

## Summary

| Round | Tier | Agent Calls | New Tests | Files Modified |
|-------|------|-------------|-----------|----------------|
| R1 | MEDIUM (A2+A3) | ~4 | ~18 | 7 |
| R2 | DIRECT | ~2 | ~8 | 4 + 1 new |
| R3 | TEST-ONLY | ~2 | ~10 | 0 (tests only) |
| **Total** | | **~8** | **~36** | |

**Target test count**: ~1,084 (1,048 + 36)
