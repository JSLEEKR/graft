# Graft v3.5 Implementation Plan

**Date**: 2026-04-02
**Source**: v3.4→v3.5 retro_technical.md + retro_process.md
**Process**: R-PROC-13 (MEDIUM=ambiguity), R-PROC-14 (wait for analysis), R-PROC-15 (A3 backlog)

## A3 Backlog Items (from v3.4-R1)

- v3.4-R1-A3-01: collectRenameLocations matches names inside comments and strings
- v3.4-R1-A3-02: CRLF line ending handling
- v3.4-R1-A3-03: newName validation
- v3.4-R1-A3-04: Import path string false matches

## Rounds

### R1: Rename Hardening (DIRECT)

**Justification for DIRECT**: All fixes are spec-constrained. isInComment/isInString already exist and just need extraction + reuse. No design ambiguity per R-PROC-13.

**Scope**:
- Extract isInComment(), isInString() from completions.ts to utils.ts
- Update completions.ts to import from utils.ts
- Add skip-region filtering to collectRenameLocations (comments, strings, import paths)
- CRLF: normalize `\r\n` to `\n` before processing
- Add newName validation in server.ts rename handler: /^[A-Za-z_][A-Za-z0-9_]*$/ + keyword blacklist

**Test target**: ~14 new tests

### R2: Cross-file Conflicts + Handler Extraction (DIRECT)

**Scope**:
- Cross-file conflict detection: parse affected files, check for name collisions
- Extract rename handler logic to pure function in features/rename.ts
- R-PROC-12 applied: two items merged

**Test target**: ~10 new tests

### R3: FlowNode Location + Symbol Enhancement (DIRECT)

**Scope**:
- Add SourceLocation to FlowNode 'node' kind in parser
- Update AST type: `{ kind: 'node'; name: string; location?: SourceLocation }`
- Add parallel/foreach children to document symbols
- R-PROC-12 applied

**Test target**: ~8 new tests

### R4: Integration + Regression Tests (TEST-ONLY)

**Test target**: ~10 new tests

## Budget

| Round | Tier | Est. Agent Calls | Est. Tests |
|-------|------|-----------------|------------|
| R1 | DIRECT | 2 | 14 |
| R2 | DIRECT | 2 | 10 |
| R3 | DIRECT | 2 | 8 |
| R4 | TEST-ONLY | 2 | 10 |
| **Total** | | **~8** | **~42** |

**Test target**: 690 + ~42 = ~732 tests
