# Graft v4.4 Implementation Plan

## Overview

Expression extraction + string interpolation + registry enrichment + memory archival. 4 rounds, ~8 agent calls estimated.

## Rounds

### R1: evaluateExpr Extraction (DIRECT)

**New file**:
- `src/runtime/expr-eval.ts` — evaluateExpr, resolveNestedField, builtin dispatch

**Files to modify**:
- `src/runtime/flow-runner.ts` — Remove evaluateExpr + resolveNestedField, import from expr-eval.ts, re-export for backward compatibility
- `src/runtime/transforms.ts` — Import resolveNestedField from expr-eval.ts (if used)

**Test file**: `tests/v44-r1.test.ts`

**Test targets** (~6 tests):
- Import evaluateExpr from expr-eval.ts works
- Import evaluateExpr from flow-runner.ts still works (re-export)
- Import resolveNestedField from expr-eval.ts works
- Existing evaluateExpr behavior preserved (basic smoke tests)
- All existing tests continue passing (regression)

### R2: String Interpolation (MEDIUM, A2+A3)

**Files to modify**:
- `src/lexer/tokens.ts` — Add TemplateString token type (or reuse String with template flag)
- `src/lexer/lexer.ts` — Detect `${` inside string literals, produce template tokens
- `src/parser/ast.ts` — Add template Expr kind + TemplatePart type
- `src/parser/parser.ts` — parsePrimary: handle template strings, parse inner expressions
- `src/runtime/expr-eval.ts` — evaluateExpr: template case (evaluate parts, concatenate)
- `src/analyzer/types.ts` — inferExprType: template → 'string', recurse into expr parts
- `src/analyzer/graph-checker.ts` — checkExprSources: recurse into template expr parts

**Test file**: `tests/v44-r2.test.ts`

**Test targets** (~18 tests):
- Lex: template string with interpolation
- Parse: simple template `"hello ${name}"`
- Parse: multiple interpolations `"${a} and ${b}"`
- Parse: expression inside interpolation `"${A.score * 2}"`
- Parse: function call inside `"${len(A.items)}"`
- Parse: escaped `\${` → literal text
- Parse: empty template (no interpolations) → regular string literal
- Evaluate: simple substitution
- Evaluate: numeric auto-conversion
- Evaluate: nested field access in template
- Evaluate: function call in template
- Type: template infers to 'string'
- Scope: template with valid field access
- Scope: template with invalid field → error
- Integration: let binding with template compiles
- Integration: template at runtime in flow

### R3: BUILTIN_FUNCTIONS Enrichment + Memory Archival (DIRECT)

**Files to modify**:
- `src/parser/ast.ts` — Extend BUILTIN_FUNCTIONS with returnType, description, signature
- `src/analyzer/types.ts` — inferExprType call case reads from BUILTIN_FUNCTIONS.returnType
- `src/lsp/features/hover.ts` — Read signature + description from BUILTIN_FUNCTIONS, remove FUNC_DOCS

**New file**:
- `harness/archived_ratchets.md` — T1-v2.2 ratchets moved from common_memory.md

**Files to modify**:
- `harness/common_memory.md` — Replace T1-v2.2 sections with archive pointer

**Test file**: `tests/v44-r3.test.ts`

**Test targets** (~8 tests):
- inferExprType reads returnType from registry (not hardcoded)
- Hover docs generated from registry (not FUNC_DOCS)
- All 7 builtins still work (regression)
- Type inference unchanged for all builtins
- BUILTIN_FUNCTIONS type shape validated

### R4: Integration + Regression Tests (TEST-ONLY)

**Test file**: `tests/v44-r4.test.ts`

**Test targets** (~10 tests):
- Cross-feature: template with function call inside
- Cross-feature: template in let binding used in condition
- Cross-feature: template with multiplication/modulo inside
- Regression: all existing expression types still work
- Regression: imports from flow-runner.ts still work after extraction
- Scale: complex pipeline with templates + all operators + all builtins compiles

## Summary

| Round | Tier | Agent Calls | New Tests | Files Modified |
|-------|------|-------------|-----------|----------------|
| R1 | DIRECT | ~1 | ~6 | 2 + 1 new |
| R2 | MEDIUM (A2+A3) | ~4 | ~18 | 7 |
| R3 | DIRECT | ~1 | ~8 | 3 + 1 new |
| R4 | TEST-ONLY | ~2 | ~10 | 0 (tests only) |
| **Total** | | **~8** | **~42** | |

**Target test count**: ~1,119 (1,077 + 42)
