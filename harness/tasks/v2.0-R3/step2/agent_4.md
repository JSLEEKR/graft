# A4-Specialist Cross-Critique — v2.0-R3

MAINTAINS collision detection. Single checkDuplicateNames function (not A3's two-function split). Runs before checkNodeReads. Adopts A3's differentiated writes errors (one extra map lookup). Standard compiler phase ordering.
